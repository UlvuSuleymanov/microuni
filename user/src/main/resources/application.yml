eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka

server:
  port: 8090
spring:
  application:
    name: user
  config:
    import: optional:configserver:http://localhost:8888